apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: terra-001
  namespace: default
spec:
  storageClassName: ssd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 60Gi
