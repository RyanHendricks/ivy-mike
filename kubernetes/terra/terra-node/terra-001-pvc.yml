apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: terra-001
  namespace: default
spec:
  storageClassName: faster
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 80Gi
